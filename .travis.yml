language: scala
jdk: openjdk11
scala: 2.13.1
cache:
  directories:
    - $HOME/.cache/coursier
    - $HOME/.ivy2/cache
    - $HOME/.sbt
script:
  - sbt validate
after_success:
  - bash <(curl -s https://codecov.io/bash)
